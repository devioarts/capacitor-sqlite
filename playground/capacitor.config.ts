import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.devioarts.capacitor.sqlite',
  appName: 'cap-sqlite',
  webDir: 'dist',
};

export default config;
